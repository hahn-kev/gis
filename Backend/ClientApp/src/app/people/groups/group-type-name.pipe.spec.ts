import { GroupTypeNamePipe } from './group-type-name.pipe';

describe('GroupTypeNamePipe', () => {
  it('create an instance', () => {
    const pipe = new GroupTypeNamePipe();
    expect(pipe).toBeTruthy();
  });
});
