import { OrgChainPipe } from './org-chain.pipe';

describe('OrgChainPipe', () => {
  it('create an instance', () => {
    const pipe = new OrgChainPipe(null);
    expect(pipe).toBeTruthy();
  });
});
