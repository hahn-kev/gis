import { ToolbarContentDirective } from './toolbar-content.directive';

xdescribe('ToolbarContentDirective', () => {
  it('should create an instance', () => {
    // const directive = new ToolbarContentDirective();
    // expect(directive).toBeTruthy();
  });
});
