import {BottomSheetDirective} from './bottom-sheet.directive';

describe('BottomSheetDirective', () => {
  it('should create an instance', () => {
    const directive = new BottomSheetDirective();
    expect(directive).toBeTruthy();
  });
});
