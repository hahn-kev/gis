import { QuickAddDirective } from './quick-add.directive';

describe('QuickAddDirective', () => {
  it('should create an instance', () => {
    const directive = new QuickAddDirective();
    expect(directive).toBeTruthy();
  });
});
