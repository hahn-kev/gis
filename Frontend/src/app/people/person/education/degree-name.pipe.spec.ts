import { DegreeNamePipe } from './degree-name.pipe';

describe('DegreeNamePipe', () => {
  it('create an instance', () => {
    const pipe = new DegreeNamePipe();
    expect(pipe).toBeTruthy();
  });
});
