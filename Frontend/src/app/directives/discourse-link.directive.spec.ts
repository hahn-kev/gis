import { DiscourseLinkDirective } from './discourse-link.directive';

describe('DiscourseLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new DiscourseLinkDirective();
    expect(directive).toBeTruthy();
  });
});
