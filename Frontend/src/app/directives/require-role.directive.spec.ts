import { RequireRoleDirective } from './require-role.directive';

describe('RequireRoleDirective', () => {
  it('should create an instance', () => {
    // const directive = new RequireRoleDirective();
    // expect(directive).toBeTruthy();
  });
});
