import { AppTemplateContentDirective } from './app-template-content.directive';

describe('AppTemplateContentDirective', () => {
  it('should create an instance', () => {
    const directive = new AppTemplateContentDirective();
    expect(directive).toBeTruthy();
  });
});
