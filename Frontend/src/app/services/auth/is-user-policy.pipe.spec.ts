import { IsUserPolicyPipe } from './is-user-policy.pipe';

describe('IsUserPolicyPipe', () => {
  it('create an instance', () => {
    const pipe = new IsUserPolicyPipe();
    expect(pipe).toBeTruthy();
  });
});
