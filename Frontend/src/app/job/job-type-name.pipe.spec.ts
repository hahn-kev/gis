import { JobTypeNamePipe } from './job-type-name.pipe';

describe('JobTypeNamePipe', () => {
  it('create an instance', () => {
    const pipe = new JobTypeNamePipe();
    expect(pipe).toBeTruthy();
  });
});
