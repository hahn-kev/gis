import { PickFileDirective } from './pick-file.directive';

describe('PickFileDirective', () => {
  it('should create an instance', () => {
    const directive = new PickFileDirective();
    expect(directive).toBeTruthy();
  });
});
